---
title: "The observed data are modeled as the output of a graph convolutional filter, specifically "
category: "Synteza"
tags: zjisteni, pending, general
---

# The observed data are modeled as the output of a graph convolutional filter, specifically 

The observed data are modeled as the output of a graph convolutional filter, specifically a polynomial in a local diffusion graph-shift operator with unknown coefficients.

**Status:** pending · **Téma:** general · **Judge:** 0.88  
Z běhu [[run-40]].
