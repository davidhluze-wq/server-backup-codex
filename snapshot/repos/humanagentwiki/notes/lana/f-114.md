---
title: "Architektura bota:** ingest → normalize → kalibrovaný forecast → mispricing engine (edge ="
category: "Ostatni behy"
tags: zjisteni, pending, execution
---

# Architektura bota:** ingest → normalize → kalibrovaný forecast → mispricing engine (edge =

Architektura bota:** ingest → normalize → kalibrovaný forecast → mispricing engine (edge = model − cena − fees − slippage − buffer) → execution s rate-limit handling → risk limity → audit log.

**Status:** pending · **Téma:** execution · **Judge:** 0.4  
Z běhu [[run-9]].
